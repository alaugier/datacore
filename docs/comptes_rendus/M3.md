# Compte rendu de milestone — M3 Data Lake OMEGA LAKE (Bloc 4)

Rédigé le 23/09/2026, à la clôture de l'issue #66 (C21) — dernière
issue du milestone M3.

**M3 est clos** : les 4 compétences du Bloc 4 (C18 à C21) ont chacune un
livrable produit et mergé sur `dev`.

---

## 1. Livrables produits

| Livrable | Chemin |
|---|---|
| Séquencement du bloc et justification (avant codage) | `docs/architecture/sequencement_bloc4.md` |
| Architecture du data lake (C18) | `docs/architecture/architecture_omega_lake.md`, `docs/architecture/images/` |
| Intégration infrastructure (C19) | `docs/architecture/integration_infrastructure_omega_lake.md`, `src/datacore/storage/lake/{ingestion_batch,sse_consumer}.py`, services `minio`/`minio-init` (`docker-compose.yml`) |
| Catalogue de données (C20) | `docs/architecture/catalogue_omega_lake.md`, `src/datacore/storage/lake/{transform,catalogue}.py` |
| Gouvernance des données (C21) | `docs/architecture/registre_rgpd_lake.md`, `src/datacore/storage/lake/{purge,curated_bi}.py`, `datacore.governance.audit_rgpd.auditer_lake()`, `infra/minio/lake-reader-policy.json` |
| Vocabulaire et orientation (transverse, non RNCP) | `docs/architecture/organisation_omega_lake.md` — hiérarchie bucket/zone/flux, demandé après relecture |
| Notebooks de vérification/exploration (transverse) | `notebooks/verification_duckdb_minio_omega_lake.ipynb`, `notebooks/exploration_omega_lake.ipynb` |
| Aide-mémoire technique (transverse) | `docs/reference_technique/python.md` — boto3, DuckDB, débuté ce jalon |

## 2. Compétences couvertes et preuves associées

| Compétence | Statut | Preuve |
|---|---|---|
| C18 — Concevoir une architecture de données pour le big data | Couverte | Zones `raw`/`staging`/`curated`, articulation avec staging/entrepôt tranchée (flux séparé en entrée, jointure à la lecture en sortie) sur un point explicitement laissé ouvert par M2.md §5 ; clés de jointure réelles vérifiées dans les données, pas supposées |
| C19 — Intégrer une infrastructure de données à partir de composants | Couverte | MinIO déployé, ingestion batch (fidélité vérifiée par SHA-256), consommateur SSE (robustesse vérifiée par coupure brutale réelle, `kill -9` + redémarrage) ; mécanisme de jointure DuckDB tranché et vérifié contre MinIO/Postgres réels avant tout code de pipeline |
| C20 — Développer un catalogue de données | Couverte | Périmètre explicitement élargi (construire un contenu réel, pas seulement cataloguer) ; catalogue **généré par introspection** (boto3 + DuckDB `DESCRIBE`), pas documentation statique ; pipeline complet vérifié de bout en bout, 0 écart de volumétrie sur les 5 flux |
| C21 — Garantir la gouvernance des données | Couverte | Registre RGPD du lake (audit lexical automatisé + traitement géolocalisation formalisé), procédure de purge vérifiée par injection d'une partition synthétique réellement purgée, droits d'accès par groupe implémentés et corrigés après relecture externe (politique restreinte à un export dédié, `vehicule_id` pseudonymisé par HMAC, vérifié bout en bout avec les vraies clés `lake_reader`) |

## 3. Décisions techniques et justifications

- **Trois couches, trois logiques** : la différence entre base de
  staging, entrepôt et data lake n'est pas le volume ni le degré de
  nettoyage, mais le moment où le schéma est décidé (*schema-on-write*
  pour les deux premiers, *schema-on-read* pour le lake) — documenté en
  C18 après une question de compréhension, avec un schéma dédié.
- **Ingestion en flux séparé, jointure à la lecture** : les flux IoT ne
  transitent jamais par la base de staging relationnelle, mais la zone
  `curated/` est conçue pour être jointe à la volée aux dimensions
  conformées de l'entrepôt (`dim_site`, `dim_produit`) via des clés
  naturelles réelles — pas de duplication entre les briques de stockage.
- **DuckDB retenu comme mécanisme de jointure**, tranché explicitement
  avant tout code de pipeline (demandé en relecture externe). Le projet
  n'utilisant `pandas` nulle part (vérifié par grep avant de décider),
  DuckDB (SQL direct, embarqué) s'est révélé le choix cohérent avec
  l'existant, pas seulement une préférence technique. Une réserve
  soulevée en relecture (preuve initiale contre un Parquet local, pas
  contre MinIO) a été levée par une seconde vérification en conditions
  réelles.
- **Ingestion `raw/` via `boto3`, transformations via DuckDB** : deux
  outils pour deux rôles distincts — une copie fidèle octet pour octet
  ne doit pas repasser par un moteur qui réinterprète le contenu.
- **Consommateur SSE fenêtré par intervalle (5 min), pas par jour** : le
  flux source n'étant pas rejouable, la perte réelle en cas de coupure
  est bornée à l'intervalle. Vérifié par une coupure brutale réelle
  (`kill -9` en cours de réception, redémarrage, aucune collision ni
  donnée corrompue) — point explicitement demandé en relecture externe,
  traité par un test réel plutôt qu'une description du comportement
  attendu.
- **Périmètre de C20 élargi explicitement avant de démarrer** : le texte
  initial de l'issue #65 ne parlait que de cataloguer « après C19 » —
  or après C19, `staging/`/`curated/` restaient vides. Documenté et
  tranché dans `sequencement_bloc4.md` §4 avant tout code, même logique
  de vigilance que l'écart C4/C6/C7 découvert à la clôture de M0.
- **Catalogue généré par introspection réelle** (`boto3` +
  `DESCRIBE`), pas maintenu à la main — même principe que
  `audit_rgpd.py` (C16) : un catalogue généré ne peut pas devenir
  silencieusement obsolète.
- **Risque de ré-identification trouvé en concevant, pas en auditant a
  posteriori** : `vehicule_id` (géolocalisation) se recoupe réellement,
  dans le temps, avec `tournees.chauffeur` de la base de staging.
  Décision actée dès C18 : aucune jointure automatisée entre les deux —
  la mesure porte sur le risque réel, pas sur l'identifiant isolément.
- **Pseudonymisation de `vehicule_id` implémentée après relecture
  externe**, dans un second temps (C21bis) : la première analyse
  (« pas de protection réelle possible ») s'est révélée incomplète —
  vraie pour les Data Engineers (accès complet des deux côtés), fausse
  pour `lake_reader` (accès lake seul, sans la base de staging), pour
  qui un pseudonyme HMAC à clé secrète constitue une vraie défense en
  profondeur. Implémenté comme un export dédié (`curated_bi/`, pas de
  modification de `curated/`), avec la clé jamais distribuée à
  `lake_reader`. Un vrai hash sans clé aurait été insuffisant (15
  véhicules seulement, recalculable par force brute) — HMAC retenu
  explicitement pour cette raison.
- **Droits d'accès par groupe implémentés, pas seulement documentés,
  et corrigés en cours de route** : la politique `lake_reader` accordée
  en C21 portait sur le bucket entier — vérifié que cela permettait de
  lire `raw/`/`staging/`/`curated/` en clair, ce qui aurait rendu la
  pseudonymisation de `curated_bi/` contournable. Restreinte à ce seul
  préfixe, revérifiée bout en bout avec les vraies clés `lake_reader`
  (lecture de `curated_bi/` réussit et renvoie des données
  pseudonymisées ; la même requête contre `curated/` échoue en 403).
- **Faille trouvée en seconde relecture externe, après le merge de la
  PR C21bis, puis corrigée dans une PR dédiée** : `config.py` fournissait
  un repli codé en dur pour `LAKE_PSEUDONYM_KEY`
  (`"datacore_pseudonym_key_dev_only"`), visible dans le dépôt public —
  un déploiement qui oublierait de définir la vraie clé aurait utilisé
  silencieusement cette valeur connue, rendant la pseudonymisation
  inopérante sans le moindre avertissement. Vérifié avant correction
  (import de `datacore.config` sans la variable définie → repli utilisé
  sans erreur). Corrigé : `config.py` ne fournit plus aucun repli pour
  cette variable (`os.environ.get()` sans défaut, contrairement au
  reste du module — la suggestion littérale d'un accès strict par
  crochets a été écartée après vérification, car elle aurait fait
  échouer l'import de `config.py` pour tous les scripts qui n'utilisent
  jamais cette clé) ; `verifier_cle_configuree()` refuse désormais
  explicitement de continuer si la clé est absente, vaut une valeur
  publique connue (exemple de `.env.example`, ou l'ancien repli
  supprimé), ou fait moins de 32 caractères. Les deux cas vérifiés en
  conditions réelles (`RuntimeError`, code de sortie non nul).
- **Purge RGPD vérifiée par un test réel, pas simulé sur papier** : une
  partition synthétique délibérément datée a été injectée dans le lake
  réel, purgée avec succès, et le résultat confirmé indépendamment
  (`boto3`, hors du processus de purge).

## 4. Écarts par rapport au cahier des charges

- **Périmètre de C20** : l'issue #65 ne mentionnait explicitement que le
  catalogage ; élargi pour inclure la construction du contenu
  `staging/`/`curated/` lui-même, sans quoi le catalogue n'aurait porté
  que sur `raw/`. Décision documentée avant codage
  (`sequencement_bloc4.md` §4), pas découverte a posteriori.
- **Mesure de protection RGPD (C21)** : la pseudonymisation envisagée
  par le plan initial de C3 a d'abord été écartée sur une analyse
  incomplète, puis réintroduite après relecture externe — implémentée
  comme défense en profondeur pour `lake_reader` spécifiquement, en
  complément (pas en remplacement) de l'absence de jointure automatisée
  actée en C18 (voir §3).
- Aucun autre écart identifié : les 4 compétences C18 à C21 attendues
  pour le Bloc 4 (cahier des charges §6.1) ont chacune un livrable
  conforme à la description attendue.

## 5. Points ouverts / risques pour la suite (M4 — Rapport LaTeX, M5 — Soutenance)

- Le prestataire SupervIT, prévu pour l'intégration technique du Bloc 4
  (`supervision_projet.md`), n'a en pratique pas été mobilisé — le
  périmètre pédagogique n'en a pas eu besoin, à signaler comme écart
  budgétaire/organisationnel si le rapport final en fait la synthèse.
- MinIO/le consommateur SSE tournent en local via Docker Compose, sans
  supervision de processus réelle (relance automatique en cas d'arrêt).
  Suffisant pour la démonstration pédagogique, à traiter comme un
  prérequis explicite si le programme évoluait vers un déploiement réel.
- La purge RGPD (`purge.py`) n'est pas planifiée automatiquement
  (aucun ordonnanceur actif, même limite déjà notée pour les sauvegardes
  de l'entrepôt en `M2.md` §5) — à exécuter manuellement ou via cron en
  conditions réelles.
- **Stabilité de `LAKE_PSEUDONYM_KEY` entre exécutions, non garantie ni
  surveillée** : rien dans le code n'écrit `.env` ni ne modifie cette
  variable (vérifié), mais une action humaine (rotation volontaire,
  réédition de `.env`, clone avec sa propre valeur générée localement)
  peut la faire changer d'un run à l'autre. `pseudonyme()` continuerait
  de fonctionner avec la nouvelle clé, mais produirait, pour le même
  `vehicule_id`, un pseudonyme différent de celui des exports
  `curated_bi/` précédents — rompant silencieusement la continuité
  analytique, sans détection ni avertissement (aucune empreinte de clé
  journalisée). Risque limité pour un poste de développement unique,
  à traiter avant tout déploiement multi-environnements réel.
- Le programme (M0 à M3) est maintenant fonctionnellement complet sur
  les 4 blocs techniques (C1-C21) ; M4 (rapport LaTeX) et M5
  (soutenance) restent les deux jalons transverses, différés par choix
  jusqu'ici (cf. `plan_de_developpement.md`) — la matière technique pour
  les rédiger existe désormais intégralement dans `docs/`.
